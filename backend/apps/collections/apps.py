from django.apps import AppConfig


class CollectionsConfig(AppConfig):
    """Collections ``AppConfig`` subclass."""

    name = 'apps.collections'
    verbose_name = 'App for Star Wars API resource.'
